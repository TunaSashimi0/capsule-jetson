# Keep the canonical source dataset available on fresh clones without bloating
# normal Git history. Labels and metadata remain ordinary text files.
/labeled_data/images/** filter=lfs diff=lfs merge=lfs -text
/models/trained/capsule_yolo11s_obb_best.pt filter=lfs diff=lfs merge=lfs -text
