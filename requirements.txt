# Local/desktop installation. Application dependencies are shared exactly with
# Docker; accelerator and OpenCV builds are platform-specific.
-r requirements.shared.txt

torch==2.11.0
torchvision==0.26.0
opencv-python==4.11.0.86
